{
  "routes": [
    {
      "method": "GET",
      "path": "/order-ratios",
      "handler": "order-ratios.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/order-ratios/count",
      "handler": "order-ratios.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/order-ratios/:id",
      "handler": "order-ratios.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/order-ratios",
      "handler": "order-ratios.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/order-ratios/:id",
      "handler": "order-ratios.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/order-ratios/:id",
      "handler": "order-ratios.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
